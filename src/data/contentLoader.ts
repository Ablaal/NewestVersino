import siteContent from '../../content/siteContent.json';
import type { SiteContent } from '@/types';

export const content: SiteContent = siteContent as unknown as SiteContent;

export default content;
